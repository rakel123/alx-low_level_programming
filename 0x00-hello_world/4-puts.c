#include <stdio.h>

/**
 * main - Entry point
 * Description: A file to print a word
 * Return: always 0 (success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
